settings:
  APP_GROUP_IDENTIFIER: group.io.pesenger.pr
  BASE_BUNDLE_IDENTIFIER: io.pesbc.pesenger.pr

targets:
  ElementX:
    settings:
      base:
        ASSETCATALOG_COMPILER_APPICON_NAME: AlphaAppIcon
    sources:
      - path: Resources
